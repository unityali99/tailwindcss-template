const Icon = ({ icon }) => (
  <div className="inline-block mx-4 cursor-pointer text-blue-300 hover:text-pink-500 transition-all duration-150 scale-75 hover:scale-100 ease-linear">
    {icon}
  </div>
);

export default Icon;
